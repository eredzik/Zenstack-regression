/**
 * Placeholder "enhance" for demos: returns the raw Prisma client.
 * For real v2 vs v3 regression tests, replace with two modules that re-export
 * each version's `enhance` from your installed ZenStack packages.
 */
export function enhance(prisma) {
  return prisma;
}
